# 2026-08-15 工作日志

## Render 回滚陷阱（重要）
- 现象：GitHub 远端 master 早已在最新提交 `8e91fb7`（含 Tier1 折叠工具栏 + 复制按钮 + 三个生成器），但线上 `https://xinke-peixun-ai.onrender.com` 仍显示旧版。
- 原因：用户（或同事）在 Render 后台点了 **Rollback**，把服务钉在旧部署 `88f1ddc`。Rollback 独立于 GitHub，不随代码走。
- 关键：GitHub 无新提交时，`Auto-Deploy` **不会**自动触发。必须到 Render Dashboard 该服务 → **Manual Deploy → Deploy latest commit** 才会用 GitHub 当前 HEAD 重新部署、覆盖回滚。
- 教训：上线前确认 Render 没有被 Rollback 钉死；若已回滚，推代码没用，要先 Manual Deploy latest commit。

## 上线验证通过
- 用户按指引去 Render Dashboard 点 Manual Deploy → Deploy latest commit，部署 `dep-d9vtbkb1560a73buau6g` 成功（08-15 08:41）。
- 抓 `https://xinke-peixun-ai.onrender.com` 验证：标题「泌园春AI小助手」、7 个工具按钮（tRegen/tExport/tSrc/tClear/tScript/tFlow/tCard）、genSheet/genMask 生成器弹层、toast、复制按钮（动态创建 className=mact）全部存在。HTTP 200 / 18 KB / 0.7s。
- 部署完成后建议用户 Delete GitHub 上已暴露的 PAT `ghp_zv4...`。

## 凭据
- 用户新 PAT `ghp_zv4...` 经 GitHub API 验证有效，归属 `2647287686-coder`（仓库所有者）。推送命令格式：用户名用账号名、PAT 作密码：`https://2647287686-coder:<PAT>@github.com/2647287686-coder/xinke-peixun-ai.git`。
- 旧 PAT `ghp_9Z2...` 实测 401 Bad credentials（已失效/被撤销），作废。
