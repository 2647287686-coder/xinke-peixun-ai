# 2026-08-19 工作日志

## 资料库同步上线进展
- 同步 WorkBuddy「新员工培训」空间最新内容到 `kb/text/`（kb/sync.py，token 来自 connect_open_platform skill_id=library），得 16 篇（原 14，新增 抖音本地生活团购三种模式.txt、肉丸话术.txt，修订 喜客丸品牌定位.txt）。
- 本地验证 kb_chunks 96→115 正常；提交 `f0d961a`。
- 改用 SSH 部署密钥打通自动推送：生成 `C:/Users/admin/.ssh/id_ed25519_github`（无密码），公钥已登记到 GitHub（Settings→SSH and GPG keys）。remote 改为 `git@github.com:2647287686-coder/xinke-peixun-ai.git`。SSH 推送 `8e91fb7..f0d961a` 成功，不再依赖 PAT。
- 验证发现：**线上 Render 仍跑旧部署 8e91fb7（14 篇）**，目标问题来源里查不到两个新文件。GitHub 端 f0d961a 正确，但 Render 自动部署未顶上来。
  - 注意：`/api/health` 的 `kb_chunks` 是懒加载（首次提问才 load_kb），未提问前恒为 0，不能仅凭 health=0 判断 KB 失效。
  - 待办：需用户在 Render 后台 Manual Deploy latest commit（现为 f0d961a）强制上线，或查 Deploys 页确认是否构建失败。

## 待办
- 用户手动触发 Render 重新部署 f0d961a 后，我再去抓线上 /api/ask 来源验证两个新文件是否生效。

## 部署验证结果（用户 Manual Deploy 后）
- 线上 /api/ask 来源已含「抖音本地生活团购三种模式」「肉丸话术」等新文件，回答正文成功引用新内容（如枚举抖音团购三种模式）。f0d961a 部署成功，16 篇 KB 全部生效。
- 用户确认: SSH 密钥部署方式可行, 以后同步/改代码上线走 SSH 推送 + Render 手动部署即可, 不再依赖 PAT。

## 长期记忆新增
- 用户回复「1」=「已执行操作/已完成该操作」, 已写入 ~/.workbuddy/MEMORY.md 沟通约定。

## 第二次资料库同步上线验证（bd93c2a，用户回「1」确认 Manual Deploy）
- 本次同步新增 1 篇: 「营业执照注册码被占用与门店已被认领该如何解决」; 总计 17 篇 / 123 片段。
- SSH 推送成功 f0d961a..bd93c2a, 远端 HEAD=bd93c2a。
- 用户 Manual Deploy 后线上验证: /api/ask 来源含「营业执照注册码被占用与门店已被认领该如何解决」(top 匹配) 及「抖音本地生活团购三种模式」, 17 篇 KB 全部生效。
- 流程定型: 用户说"更新资料库"→ 我 sync+commit+SSH推送 → 用户 Render Manual Deploy 回「1」→ 我抓线上验证。


## 改名与间距迭代
- 助手名「泌园春AI小助手」→「喜客丸AI小助手」: 改 static/index.html 的 title/头部/问候/导出标题 4 处 + app.py SYSTEM_PROMPT 1 处（提交 c37e993），线上验证标题与头部已更新。
- 回复气泡间距太散,用户反馈后连续迭代两版:
  - a7aa2e0: p margin .35em->.18em，line-height 1.6->1.5 等。Manual Deploy 轮询发现线上仍是旧 CSS——Render 没替换文件,需重触发或确认 Manual Deploy 真生效。
  - dd8d8c4: 进一步将 p margin 降至 .08em，列表 li margin 0;再次部署发现仍未生效。
  - 0c014b9 (最新): 直接处理空段堆叠:(a) render 前用正则把 3+ 连续换行压成 2，移除尾随空白;(b) CSS 让段落默认 margin:0,仅在「p+p / p+ul / p+h」相邻时给 .18em;(c) 空段落 display:none;(d) <br> line-height 降到 1 避免 marked breaks:true 模式下累积空白。本地 Edit 工具多次报错 undefined,改用 python3 -c 内联替换 + git commit/push。
- 教训: Render Manual Deploy 似乎有时没真正替换文件（轮询几轮还看到旧规则）,所以部署后**一定要轮询验证**,不能假设。本次改用 grep -c 检测新规则出现而非仅看 HTTP 200。

## 输出格式约定：同类枚举项单行输出（4f7cc59）
- 用户反馈: AI 回答配料/清单时把每项都另起一行成列表, 截图里「猪肉/花胶/五指毛桃粉...」期望一行逗号/顿号连接。
- 修改: app.py SYSTEM_PROMPT 加第 8 条规则, 要求小黄把「同类并列项」(配料、清单、要点等) 默认用「、」/「，」连成一行输出, 列表只留给步骤/不同维度说明/需强调排序的场景。
- 提交 4f7cc59, SSH 推送 0c014b9..4f7cc59, 待用户 Manual Deploy。

## 枚举单行输出验证（用户 Manual Deploy 后，4f7cc59）
- 线上实测: 问「五指毛桃海参肉丸的配料」→ 单行顿号连接(猪肉、加拿大北极红参、五指毛桃粉、木薯淀粉、食用盐、白砂糖) ✅ 符合诉求。
- 但问「喜客丸肉丸的主要配料」→ 小黄用 - 列表块分「主料/药食同源/辅料」三行, 虽然各维度内枚举项单行, 整体仍是多行 bullet。
- 强化规则(提交 c55e0b9, 加第 9 条): 分维度说明也用「**小标题**:内容。」单行紧凑写法, 禁用 -/\* 列表块; 仅步骤型操作/需排序才用列表。已 SSH 推送, 远端 HEAD=c55e0b9, 待用户再次 Manual Deploy。
- 解析 SSE 注意: token 格式是  (JSON 字符串), 不是 ; 用 python 解析需 isinstance(v,str) 直接 append。

## 新增「账号体系 + 总部后台」功能（commit 450b346）
- 需求：放弃微信小程序方案；改做手机号+密码账号体系（无短信，密码找回由总部主账号后台重置）+ 总部后台。
- 选型确认：手机号+密码（无短信）、免费云 Postgres、同应用内嵌 /admin。
- 新增文件：`db.py`（SQLAlchemy，DATABASE_URL 用 Postgres，未配置回退 SQLite）、`templates/admin.html`（后台页）。
- 改造 `app.py`：注册/登录/登出/me/forgot；/api/config（动态助手名+群公告）；/api/ask 需登录并埋点 usage_log；/api/admin/*（stats、balance、announcement、rename、users 列表+详情、reset-password、改角色）。`SYSTEM_PROMPT` 改为模板，助手名动态化（后台可改名）。前端 `static/index.html` 加登录/注册/忘记密码门禁 + 群公告横幅 + 动态标题。
- 关键坑：SSE 生成器在**请求上下文之外**执行，生成器内 `db.session.commit()` 报 "Working outside of application context" 导致使用记录丢失；已用 `with app.app_context():` 包裹提交修复。本地 SQLite 联调全通过（注册/登录/鉴权拦截/提问埋点/stats/改名/公告/重置密码/未登录拒答）。
- 部署须知（render.yaml 已加 env）：需设 `DATABASE_URL`(Postgres 连接串)、`SECRET_KEY`、`ADMIN_PHONE`(总部主账号手机号)、`ADMIN_DEFAULT_PASSWORD`(初始密码，启动自动建主账号)。本地联调 venv：`C:/Users/admin/.workbuddy/binaries/python/envs/default`。
- 坑：本机 8000 端口堆积多个历史测试 app.py 进程（SO_REUSEADDR 多监听），导致新旧代码混答 404/200；本地测试请换干净端口（如 8013）或先清进程。

## 账号体系提交已 SSH 推送 + 部署清单交付
- SSH 推送 `git push -u origin master` 成功：`c55e0b9..450b346 master -> master`，上游跟踪重建。
- 已核对 render.yaml：4 个新 env（DATABASE_URL/SECRET_KEY/ADMIN_PHONE/ADMIN_DEFAULT_PASSWORD）均 sync:false，需用户在 Render 后台手动填；DEEPSEEK_API_KEY/SEARCH_API_KEY 应已存在需核对。
- 已核对代码约定：db.py 自动 postgres://→postgresql://；seed_admin 启动按 ADMIN_PHONE+ADMIN_DEFAULT_PASSWORD 建主账号（仅不存在时）；/api/health 存在。
- 交付 Render 部署清单：①配 4 变量 ②Neon/Supabase 免费 Postgres ③Manual Deploy 450b346 ④部署后自检。待用户 Manual Deploy 回「1」→ 我做线上端到端验证。

## 线上验证发现的两个问题 + 修复（commit 4046ee1）
- 用户 Manual Deploy 450b346 后我抓线上验证：
  1. `/api/config` 返回 **500**（HTML 错误页）——真 bug，每个员工进首页都调，影响所有人。根因：Neon 云 Postgres 断开空闲连接，SQLAlchemy 默认池无 pre-ping，首次查 settings 表命中死连接抛 OperationalError。health 不查库所以仍 200（误判正常）。
  2. 管理员登录 401：Render 环境变量 `ADMIN_PHONE/ADMIN_DEFAULT_PASSWORD` 被填成了 `13258671796/wyc16888`，但用户实际主账号是 `13250671706/myc16888`（手机号差一位、密码首字母不同）→ seed 出的主账号登录不上。
- 修复：① SQLAlchemy 引擎加 `pool_pre_ping=True` + `pool_recycle=280`；② `/api/config` 加 try/except 回滚重试兜底；③ `seed_admin` 改为「主账号以环境变量为准，已存在则同步密码+角色」——改 env + 重新部署即可随时修正主账号，无需手清旧账号。提交 4046ee1 并 SSH 推送 `450b346..4046ee1`。
- 待办：用户在 Render 把 `ADMIN_PHONE`→`13250671706`、`ADMIN_DEFAULT_PASSWORD`→`myc16888` 改对，Manual Deploy 选 4046ee1；回「1」我做端到端验证。
- Neon 连接串经验：用 **Direct connection**（host 形如 `ep-xxx.region.aws.neon.tech`），保留 `?sslmode=require` 但**删掉 `&channel_binding=require`**（Render 的 psycopg2-binary 不支持通道绑定）。池化连接（region pooler，需 endpoint 参数）在 Render 上两难，别用。

## 线上端到端验证（用户改对 env + Manual Deploy 4046ee1 后，回「1」）
- 主账号登录 `13250671706 / myc16888` 成功（role=admin），env 已改对、seed_admin 同步生效。
- `/api/config` 不再 500（修复生效），返回助手名+公告。
- 后台：stats（admins1/用户数/提问数）、users 列表（角色+手机号）、DeepSeek 余额（CNY 19.78）全部 200 ✅。
- 改名 POST /api/admin/rename（{name}）实时反映到 /api/config ✅。
- 群公告：前端发 `{text:t}`、后端读 `text`（app.py:587）一致；config 正确显示公告 ✅（此前测试误发 {announcement} 才显空，非 bug）。
- 员工注册 13900008888 → 登录 → `/api/ask` SSE 流式（done1/error0，kb5）→ 提问后 stats total_questions:1/questions_today:1 → 后台用户详情(id=3)显示 question+preview+kb5 ✅ 生产埋点闭环成立。
- 结论：账号体系+总部后台线上完全可用，无需再改代码。验证中把助手名改为「喜客丸新员工培训助手」、发了欢迎公告，并留下 2 个测试员工(13912345678、13900008888)，可清理或保留，用户定。

## 首页 JS 崩溃修复（commit 306fb4d）
- 现象：用户反馈「点注册/忘记密码/登录都没反应」。F12 Console 报 `Uncaught TypeError: Cannot set properties of null (setting 'onclick') at <anonymous>:350:17`。
- 根因：`static/index.html:350` `toolsBtn.onclick=...` 引用的折叠工具栏按钮 `id="toolsBtn"` 早已被删（HTML 里无此元素），`getElementById` 返回 null，该 TypeError 使**后续整段脚本全部终止**（atabs 切换/aSubmit 登录/aForgot 忘记密码/userBtn/bootAuth 均未执行）。
- 修复：删 `toolsBtn` 常量声明、350 行 onclick 块、5 处 `toolsBtn.click()` 调用；工具栏改为默认展开。`git grep -c toolsBtn` 已为 0。
- 校验：node 脚本比对 index.html 全部 37 个 `getElementById('id')` 引用在 HTML 中均存在 → 无缺失（排除再出现 null 崩溃）。已 SSH 推送 `4046ee1..306fb4d`，待用户 Manual Deploy 选 306fb4d，回「1」我做线上验证。
- 备注：后端 /api/* 全程未受影响（崩溃仅前端）。

## 后台新增 3 个功能（commit ab4a0cd）
- 用户要求加：①热门问题榜(Top10) ②答案反馈👍👎 ③在线管理资料库。
- db.py 新增 `Feedback` 模型（usage_log_id/user_id/rating/comment/created_at），`db.create_all()` 自动建表。
- app.py 新增 8 个 API：
  - `POST /api/feedback`（员工端，需登录，防重复提交——已存在则更新）
  - `/api/ask` done 事件增加 `log_id` 字段供前端绑定反馈
  - `GET /api/admin/hot-questions`（近30天按 question 分组 count desc limit 10）
  - `GET /api/admin/feedback`（支持 rating 筛选 up/down，关联 user+usage_log 返回 phone/question/preview）
  - KB 管理 5 个：`GET /api/admin/kb/files`、`GET /api/admin/kb/file/<fn>`、`POST .../save`、`DELETE .../delete`、`POST /api/admin/kb/reload`（调 load_kb 热重载）
  - 文件名安全正则 `^[\w\-]+\.txt$` 防路径穿越
- admin.html 新增 2 个 tab（反馈管理/知识库）+ 概览加热门问题榜 box；知识库左侧文件列表+右侧编辑器，支持新建/保存/删除/重载。
- index.html `stream()` done 事件捕获 log_id → `addFeedbackBtns()` 在消息操作区追加 👍/👎 按钮；👎 弹 prompt 收集可选评论。
- 校验：py_compile OK；Node 校验 index.html 37 个 getElementById + admin.html 25 个 $(id) 引用全部存在无 null 隐患。
- SSH 推送 `d43cb26..ab4a0cd` 成功。待用户 Manual Deploy 选 ab4a0cd，回「1」做线上验证。
- 线上验证结果（ab4a0cd）：
  - 热门问题榜 ✅ 返回 2 条（肉丸配料/个体工商户预包装）
  - 反馈 👍👎 ✅ 员工提交👎+评论 → 后台反馈列表可见（含员工名/问题/答案预览/评论）
  - KB 文件列表 ✅ 17 文件；KB 文件读取 ✅ 返回内容；KB 重载 ✅ 返回 123 chunks
  - 发现 KB 启动未加载 bug：`load_kb()` 只在 `if __name__` 里调，gunicorn 不走这段 → health 显示 kb_chunks=0
- 修复 KB 启动加载（commit 15c015a）：`load_kb()` 移到 app 初始化块，gunicorn 也能预加载。推送 ab4a0cd..15c015a。
- 待用户 Manual Deploy 选 15c015a（含 KB 启动加载修复），回「1」做最终确认。