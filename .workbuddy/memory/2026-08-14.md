# 2026-08-14 工作日志

## 新员工培训 AI 助手 — Key 配置与本地验证
- 用户提供 DeepSeek + SerpAPI Key，已写入 .env（已被 .gitignore 忽略，不会提交）。
- 修复 app.py 的 .env 加载：原先用 python-dotenv，但该包因 PyPI 超时未装上（import 静默失败），改为手写 KEY=VALUE 解析（_load_local_env()，用 os.environ.setdefault 不覆盖 Render 平台注入的变量）。
- 排查本地验证时 health.key=false：根因是多次后台启动的 app.py 旧进程没被 pkill（Git Bash 下不彻底）杀掉，端口 8000 被多个旧进程监听，请求被路由到无 .env 加载的旧代码。改用 taskkill /PID 精准清理后，单进程启动，key=true。
- 端到端验证通过：内部资料库问题精准命中知识库文档（web_used:false）；时效性问题触发 SerpAPI 真实联网核验（web_used:true，来源含政府/税务局官网），DeepSeek 真实生成带出处回答。

## 知识库自动同步改造 + 图文部署指引
- 新增 kb/sync.py：自包含同步脚本（不依赖 WorkBuddy 插件缓存），动态递归列举 workbuddy「新员工培训」空间节点、下载 docx/xlsx/pptx/txt、抽取到 kb/text；base https://www.workbuddy.cn，token 走 X-Skill-Token header，从 KB_TOKEN 环境变量或 --token 读取。实测用有效 token：39 节点 / 抽取 14 篇 / 0 失败。
- app.py 新增 /api/reload 热重载接口（更新 kb/text 后免重启）。
- 关键限制：workbuddy 资料库 open-platform token 仅 30 分钟有效（expiresIn:1800），无法在 Render 后端做无人值守永久自动同步。改用 Git 自动重部署策略：在 WorkBuddy 会话内跑 sync → git push → Render 自动重部署，员工即见最新知识库。
- 写 deploy-guide.html：图文部署指引（Render 6 步模拟界面图 + 同步策略专题 + 故障排查），已提交 git。
- 安全核查：.env（含真实 Key）已被 .gitignore 忽略、未入库；仅 .env.example 模板被跟踪。

## 代码已推到 GitHub（公网上线第一步完成）
- 用户建仓库 https://github.com/2647287686-coder/xinke-peixun-ai.git，生成 PAT（ghp_...，classic token，勾 repo 权限）。
- 本机 git push 被 Windows Schannel 证书吊销检查拦住（CRYPT_E_REVOCATION_OFFLINE）。排查记录：`-c http.schannelCheckRevoke=false` 无效；`http.sslBackend=openssl` 在沙箱里反而 DNS 解析失败（Could not resolve host）；最终用 `-c http.sslVerify=false`（Schannel）成功推送。一次性命令未把 token 写进仓库或 git 配置，凭据未缓存。
- 已 push 成功（master -> master，已设 tracking）。.env 未被推送（gitignore）。

## Render 公网部署已完成 & 上线里程碑
- 部署地址：**https://xinke-peixun-ai.onrender.com**（free 实例，Virginia 区）。
- 用户手动 Start Command: `gunicorn -w 2 -k gthread -b 0.0.0.0:$PORT app:app`。
- 我补加 gunicorn 到 requirements.txt（缺失会导致启动失败），自动重部署成功。
- **部署后首发实测的陷阱：gunicorn -w 2 多个 worker 进程独立加载 KB**，手动 /api/reload 只重载请求落到的那个 worker，其它 worker 仍是空状态（/api/health 报 kb_chunks 波动为 0/96）。修复：retrieve() 触发懒加载（if _bm25 is None: load_kb()），Start Command 改 -w 1（render.yaml / Procfile 都已更新）。修后真实问答两个场景全过：内部资料精准召回 + SerpAPI 联网核验命中（2026 社保问题命中新华网/医疗保障局/人社部等官网）。
- Windows Schannel TLS 握手 + Render 公网也遇同一问题，沙箱内用 `curl -k` 才能测到 200，业务访问不受影响（员工浏览器正常）。
- 资料更新策略：在 WorkBuddy 会话内 `python kb/sync.py --token <op_token>` → git push → Render auto-deploy (Auto-Deploy=On Commit)。

## Tier 1 前端小功能（折叠收纳 + 复制按钮）
- 改造 static/index.html：头部加 ⚙ 工具开关，下方可折叠工具栏（默认收起，max-height 过渡）收纳 4 个小功能——🔄重新生成、📤导出对话(复制全文)、📚显示参考(开关，按条展示来源标签)、🗑️清空对话。
- 每条 AI 回复下方加极简「📋 复制」操作条（复制该条回答，带 toast 提示 + execCommand 兜底）。
- 流式逻辑重构：ask()/regenerate() 复用 stream(bot,question)；sources 事件存入 bot._src，仅在 showSrc 开启时渲染来源标签（保留了「隐藏底部来源堆」的整洁原则）。
- 本地用 venv(`~/.workbuddy/binaries/python/envs/default`) 起 Flask 验证：/api/health 200(kb_chunks:96,key/web:true)、/ 200 且含 toolsBtn、SSE 流 sources→token→done 与前端解析一致。
- 已提交 commit 9f4b73a，用户选择自行 `git -c http.sslVerify=false push origin master` 触发 Render 重部署（沙箱环境 git 出网被拦且无凭据，无法代推）。

## Tier 1 生成器三件套（追加）
- 折叠工具栏再加 3 个生成器按钮：💬场景话术(tScript)、🪜流程步骤(tFlow)、🃏产品速查(tCard)。
- 点任意生成器 → 关闭工具栏、弹出底部弹层(#genSheet + #genMask 遮罩)：标题+提示+多行输入+生成/取消。输入主题后 build() 拼成结构化提示词，调用 ask() 走原检索/联网/小黄流式回答。
- 三个模板：话术=开场-介绍-利益点-促成四段；步骤卡=编号分步+注意点；速查卡=定位+卖点+人群+常见问答。均要求「基于内部资料库，不确定的标注待核实」。
- toolbar 展开高度提到 300px（7 个按钮，可滚动）；genSheet 用 transform 上滑动画。
- 本地验证：/ 含 tScript/tFlow/tCard/genSheet/genMask/genInput；/api/ask 对速查卡提示正常返回 sources(KB 命中)+token 流。已提交 commit 8e91fb7，仍由用户自行 push。

## 待用户操作（公网上线）
- 到 Render 连上面这个 GitHub 仓库自动部署 → 在 Render 环境变量填 DEEPSEEK_API_KEY / SEARCH_API_KEY。
- 将代码 git push 到 GitHub → Render 连仓库自动部署 → 在 Render 环境变量填 DEEPSEEK_API_KEY / SEARCH_API_KEY。
- 拿到 https://xxx.onrender.com 发新员工群。
- 资料更新时：让助手跑同步（或本地 python kb/sync.py --token <token>）→ git push → Render 自动重部署。
