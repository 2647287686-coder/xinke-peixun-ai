# 2026-08-14 工作日志

## 新员工培训 AI 助手 — Key 配置与本地验证

- 用户提供 DeepSeek + SerpAPI Key，已写入 `.env`（已被 `.gitignore` 忽略，不会提交）。
- 修复 `app.py` 的 `.env` 加载：原先用 python-dotenv，但该包因 PyPI 超时未装上（import 静默失败），改为手写 `KEY=VALUE` 解析（`_load_local_env()`，用 `os.environ.setdefault` 不覆盖 Render 平台注入的变量）。
- 排查本地验证时 `health.key=false`：根因是多次后台启动的 `app.py` 旧进程没被 `pkill`（Git Bash 下不彻底）杀掉，端口 8000 被多个旧进程监听，请求被路由到无 `.env` 加载的旧代码。改用 `taskkill /F /PID <pid>` 精准清理后，单进程启动，`key=true`。
- 端到端验证通过：内部资料库问题精准命中知识库文档（`web_used:false`）；时效性问题触发 SerpAPI 真实联网核验（`web_used:true`，来源含政府/税务局官网），DeepSeek 真实生成带出处回答。

## 待用户操作（公网上线）
- 将代码 `git push` 到 GitHub → Render 连仓库自动部署 → 在 Render 环境变量填 `DEEPSEEK_API_KEY` / `SEARCH_API_KEY`。
